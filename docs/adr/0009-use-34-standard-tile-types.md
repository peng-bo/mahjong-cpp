# Use 34 standard tile types

The MCR rewrite will use only the 34 standard tile types and will remove red-five tile semantics. Inputs such as `0m`, `0p`, and `0s` will no longer be legal because MCR scoring in this library excludes red dora and flower tiles.
