# Return MCR fan results

MCR score calculation will return a result shaped around recognized fans rather than riichi concepts. A result contains whether the hand is a legal win, the total fan, and the list of recognized fan entries; hands below the 8-fan minimum may still expose their fan details for debugging even though they are not legal wins.
