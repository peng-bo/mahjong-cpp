# Model MCR wall from visible self state

By default, MCR expected-score calculation will build the wall as four copies of each of the 34 standard tile types minus the player's concealed hand and melds. Callers may still provide a custom wall when they want to account for additional known unavailable tiles such as discards or other players' exposed melds.
